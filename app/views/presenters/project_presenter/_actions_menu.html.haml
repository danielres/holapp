%ul{ data: { purpose: 'actions-menu' } }
  - confirm_message = Rails.env == 'test' ? false : 'Are you sure ?'
  %li= link_to 'delete', project, method: :delete, data: { purpose: 'delete-action', confirm: confirm_message }
